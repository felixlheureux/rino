[package]
name = "rino"
version = "0.1.0"
edition = "2024"

[workspace]
members = ["kernel"]

[build-dependencies]
bootloader = "0.11"
rino-kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }